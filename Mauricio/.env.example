# Por defecto usa SQLite y no necesitas tocar nada.
DATABASE_URL=sqlite:///./libros.db

# Si algun dia quieres PostgreSQL (Neon, por ejemplo):
# DATABASE_URL=postgresql://usuario:contrasena@host/neondb?sslmode=require
